---
- name: Launch frontend
  community.docker.docker_container:
    name: frontend
    image: eliotfriedmann/tp-devops-frontend:latest
    state: started
    restart_policy: always
    pull: true
    networks:
      - name: app-network

  vars:
    ansible_python_interpreter: /opt/docker_venv/bin/python